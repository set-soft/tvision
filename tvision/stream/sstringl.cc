#ifndef NO_STREAM

#define Uses_TStringList
#define Uses_TStreamableClass
#include <tv.h>

TStreamableClass CLY_EXPORT RStringList( TStringList::name,
                              TStringList::build,
                              __DELTA(TStringList)
                            );

#endif

